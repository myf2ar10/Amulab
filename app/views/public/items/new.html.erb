<div class="container">
  <div class="row">
    <div class="col-md-7  img-hidden-m">
      <%= image_tag("knitting_is_healing_BLU.jpg", alt: "knitting is healing1", width: "100%") %>
    </div>

    <div class="col-md-5 new_item_add">
      <h4><mark>新規作品登録</mark></h4><br>
      <div>
        <% if @item.errors.any? %>
          <%= @item.errors.count %>件のエラーが発生しました
          <ul>
            <% @item.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        <% end %>
      </div>
      <%= form_with model: @item, local: true, html: { enctype: 'multipart/form-data' } do |f| %>
        <div class="col-md-12 item_edit_form">
          <p><h5><mark><%= f.label :name, "作品名" %><font color="red">*</font></mark></h5></p>
          <%= f.text_field :name %><br>
          <p><h5><mark><%= f.label :images, "画像" %></mark></h5></p>
          <%= f.file_field :images, accept: "image/*", multiple: true %><br>
          <small>
            ※なし可能。2枚以上可能。
          </small><br>
          <p><h5><mark><%= f.label :genre_id, "ジャンル" %><font color="red">*</font></mark></h5></p>
          <%= f.collection_select :genre_id, Genre.all, :id, :name %><br>
          <p><h5><mark><%= f.label :description, "作品説明文" %><font color="red">*</font></mark></h5></p>
          <small>
            ※なし可能。
          </small><br>
          <%= f.text_area :description, rows: 3 %><br>
          <%= f.submit "新規作品登録", class: "btn btn-primary" %><br>
          <small>
            ※誠に恐れ入りますが確認画面のご用意がございません。<br>
            　最後にご入力内容をご確認ください。<br>
            ※<font color="red">*</font>は入力必須項目です。<br>
          </small>
        </div>
      <% end %>
    </div>

  </div>
</div>