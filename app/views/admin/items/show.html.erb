<div class="container">
  <div class="row">

    <div class="col-md-7  img-hidden-m">
      <%= image_tag("knitting is healing1", alt: "knitting is healing1", width: "100%") %>
    </div>

    <div class="col-md-5 item_show">
      <h4>作品詳細画面</h4>

      <div class="col-md-12 item_edit_form">
        <% @item.images.each_with_index do |image, index| %>
          <%= index + 1 %><br>
          <% if image.blob.present? %>
            <%= image_tag image, style: "width: #{(1..4).include?(index) ? '300px' : '300px'};" %><br>
          <% else %>
            <%= image_tag "no_image_BLU.png", style: "width: 100px;", alt: "no_image" %><br>
          <% end %>
        <% end %>
        <b><%= label :作品名, "作品名" %></b><br>
        <%= @item.name %><br>
        <b><%= label :ジャンル, "ジャンル" %></b><br>
        <%= @item.genre.name %><br>
        <b><%= label :作品説明, "作品説明" %></b><br>
        <%= @item.description %><br>
        <b><%= label :投稿日, "投稿日" %></b><br>
        <%= @item.created_at.strftime("%Y/%m/%d %H:%M:%S") %><br>
        <p><b><%= label :投稿者, "投稿者" %></b><br>
        投稿者: <%= @item.user.name %><br></p>




        <% @item.item_comments.each_with_index do |item_comment, index| %>
          <small><b>No.<%= 1 + index %></small></b><br>
          <%= item_comment.comment %><br>
          <%= link_to user_path(item_comment.user) do %>
            <%= item_comment.user.name %>
          <% end %>
          より （<%= item_comment.created_at.strftime("%Y/%m/%d/%H:%M") %>）<br>
          <% if item_comment.user == current_user %>
            <p><%= link_to "削除", item_item_comment_path(@item, item_comment), method: :delete, class: "btn btn-danger pull-right" %></p>
          <% end %>
        <% end %>




























        <%= link_to "一覧に戻る", admin_items_path, class: "btn btn-primary" %>
      </div>

    </div>

  </div>
</div>


<!--<h1>Admin::Items#show</h1>-->
<!--<p>Find me in app/views/admin/items/show.html.erb</p>-->
<!--作品詳細画面-->