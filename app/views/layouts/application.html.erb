<!DOCTYPE html>
<html>
  <head>
    <title>Amulab</title>

    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%#= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

    <script src="https://kit.fontawesome.com/62ba821fbd.js" crossorigin="anonymous"></script>

  </head>

  <style>
  header{
    position: fixed;
    width: 100%;
    background-color: #052D37;
    padding: 20px 0;
    height: 170px;
  }
  .logo {
  margin-left: 20px;
  /* ロゴを右に10pxずらす*/
  }
  body {
    background-color: #eee9e6;
    /* E6CFDB ピンク B8C8CC ブルーグレー 052D37 紺 eee9e6 淡いベージュ E5C58F ベージュ*/
  }
  footer{
    width: 100%;
    background-color: #052D37;
    color: #fff;
    text-align: center;
    padding: 5px 0;
    /*position: absolute;*/
    /*←絶対位置*/
    position: fixed;
    bottom: 0; /*下に固定*/
  }
  </style>

  <body>

        <header class="<%= (admin_signed_in? ? "admin_header" : "" ) %>">
          <div class="header-content">

          <%= link_to(image_tag('Amulab.png', alt: 'Amulab', width: 300), (admin_signed_in? ? admin_admin_top_path : public_root_path), class: 'logo') %>
            <ul class="flex">
              <% if admin_signed_in? %>
                <%= link_to 'ジャンル一覧', admin_genres_path %>
                <%= link_to '作品一覧', admin_items_path %>
                <%= link_to 'ログアウト', destroy_admin_session_path, method: :delete %>
              <% elsif user_signed_in? %>
                <p>ようこそ、<%= current_user.full_name %>さん</p>
                <%= link_to '作品一覧', items_path %>
                <%= link_to 'ログアウト', destroy_user_session_path, method: :delete %>
                <%= link_to '新規投稿', items_path, method: :post %>
              <% else %>
                <%= link_to '作品一覧', items_path %>
                <%= link_to 'ログイン', new_user_session_path %>
                <%= link_to '新規登録', new_user_registration_path %>
              <% end %>
            </ul>

          </div>
        </header>

    <main>
      <%= yield %>
    </main>

    <footer>Copyright © Amulab All Rights Reserved.</footer>
  </body>

</html>