<!DOCTYPE html>
<html>

  <head>
    <title>Amulab/編むらぼ</title>

    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%#= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

    <script src="https://kit.fontawesome.com/62ba821fbd.js" crossorigin="anonymous"></script>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>

    <link href="https://fonts.googleapis.com/earlyaccess/roundedmplus1c.css" rel="stylesheet" />

  </head>

  <body>

    <header class="<%= (admin_signed_in? ? "admin_header" : "" ) %>">

      <nav class="navbar navbar-expand-lg navbar-light" style="background-color: #B8C8CC;">
         <!--fixed-top-->
        <a class="navbar-brand" href="#">
          <%= link_to(image_tag('Amulab3.jpg', alt: 'Amulab', width: 300), (admin_signed_in? ? public_root_path : public_root_path), class: 'logo') %>
        </a>
          <% if user_signed_in? %>
              <%= render 'layouts/user_header' %>
            <% elsif admin_signed_in? %>
              <%= render 'layouts/admin_header' %>
            <% else %>
              <%= render 'layouts/header' %>
          <% end %>
      </nav>

    </header>

    <main>
      <% flash.each do |message_type, message| %>
        <div class="flash-message <%= message_type %>"><%= message %></div>
      <% end %>
      <%= yield %>
    </main>

    <footer>
      Contact<br>
      E-mail amulabamulab@gmail.com<br>
      Copyright © Amulab All Rights Reserved.<br>
    </footer>

  </body>

</html>